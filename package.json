{
  "name": "inventory-management-system-ls",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "migrate": "prisma migrate dev --name init",
    "prisma:generate": "prisma generate",
    "seed": "ts-node /mnt/data/seed.ts"
  },
  "devDependencies": {
    "prisma": "^6.19.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0"
  }
}
